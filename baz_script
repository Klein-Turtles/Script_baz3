-- 🧩 Admin Abuse Pet Database Scraper
local Shared = require(game.ReplicatedStorage:WaitForChild("Shared"))
local ProductUtil = Shared("GameProductUtility")
local Translation = Shared("TranslationHelper")

print("--- MEMINDAI PET ADMIN ABUSE ---")

-- Kita bongkar isi tabel ProductUtility
-- Biasanya datanya ada di dalam tabel internal bernama _res atau sejenisnya
for id, data in pairs(debug.getupvalues(ProductUtil.GetResFromName)[1]) do 
    if type(data) == "table" and (data.AdminAbuseCoin or data.AdminAbuseGem) then
        -- Ambil nama asli dari Translation Helper
        local realName = Translation.TranslateByKey(data.LocName or "")
        
        print("-------------------------------")
        print("ID PRODUK: " .. tostring(id))
        print("NAMA PET : " .. tostring(realName))
        print("HARGA COIN: " .. tostring(data.AdminAbuseCoin or "N/A"))
        print("HARGA GEMS: " .. tostring(data.AdminAbuseGem or "N/A"))
        print("LIMIT BELI: " .. tostring(data.AdminAbuseBuyCount or "N/A"))
    end
end

print("--- SCAN SELESAI (Cek Console F9) ---")
